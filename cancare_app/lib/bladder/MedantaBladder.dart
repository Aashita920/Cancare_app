// ignore_for_file: file_names

import 'package:flutter/material.dart';

class MedantaBladder extends StatelessWidget {
  const MedantaBladder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
